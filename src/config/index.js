// application
const APP_VERSION = `v${process.env.REACT_APP_VERSION}`;

// BASE API URL
const API_BASE_URL = 'https://myhomeapi.local/';

// authentication
const JWT_KEY = 'jwtToken';

// Redux
const ENABLE_REDUX_LOGGER = true;

export {
    API_BASE_URL,
    APP_VERSION,
    ENABLE_REDUX_LOGGER,
    JWT_KEY
}